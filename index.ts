console.log('ok i have no idea what im doing');

const a = 'lol';

interface Track {
    title: string;
    duration: number;
    artist: string;
}

const song: Track = {
    title: 'Hand On Me',
    duration: 231,
    artist: 'Nourished by Time',
};

console.log(song);
